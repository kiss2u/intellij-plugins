// Copyright 2000-2024 JetBrains s.r.o. and contributors. Use of this source code is governed by the Apache 2.0 license.
package com.intellij.lang.javascript.flex;

import com.intellij.uiDesigner.core.GridConstraints;
import com.intellij.uiDesigner.core.GridLayoutManager;
import com.intellij.uiDesigner.core.Spacer;
import com.intellij.util.ArrayUtilRt;

import javax.swing.JComponent;
import javax.swing.JLabel;
import javax.swing.JPanel;
import javax.swing.JTextField;
import javax.swing.event.DocumentListener;
import javax.swing.plaf.FontUIResource;
import javax.swing.text.StyleContext;
import java.awt.Font;
import java.awt.Insets;
import java.util.Locale;

public class FlashPlayerVersionForm {
  private final JPanel myMainPanel;
  private final JTextField myPlayerVersionMajorTextField;
  private final JTextField myPlayerVersionMinorTextField;
  private final JTextField myPlayerVersionRevisionTextField;

  public FlashPlayerVersionForm() {
    {
      // GUI initializer generated by IntelliJ IDEA GUI Designer
      // >>> IMPORTANT!! <<<
      // DO NOT EDIT OR ADD ANY CODE HERE!
      myMainPanel = new JPanel();
      myMainPanel.setLayout(new GridLayoutManager(1, 6, new Insets(0, 0, 0, 0), 0, -1));
      myPlayerVersionMajorTextField = new JTextField();
      myPlayerVersionMajorTextField.setColumns(3);
      myPlayerVersionMajorTextField.setHorizontalAlignment(0);
      myMainPanel.add(myPlayerVersionMajorTextField,
                      new GridConstraints(0, 0, 1, 1, GridConstraints.ANCHOR_CENTER, GridConstraints.FILL_HORIZONTAL,
                                          GridConstraints.SIZEPOLICY_CAN_GROW, GridConstraints.SIZEPOLICY_FIXED, null, null, null, 0,
                                          false));
      final Spacer spacer1 = new Spacer();
      myMainPanel.add(spacer1, new GridConstraints(0, 5, 1, 1, GridConstraints.ANCHOR_CENTER, GridConstraints.FILL_HORIZONTAL,
                                                   GridConstraints.SIZEPOLICY_WANT_GROW, 1, null, null, null, 0, false));
      final JLabel label1 = new JLabel();
      label1.setAlignmentX(0.5f);
      Font label1Font = this.$$$getFont$$$(null, Font.BOLD, -1, label1.getFont());
      if (label1Font != null) label1.setFont(label1Font);
      label1.setText(".");
      myMainPanel.add(label1, new GridConstraints(0, 1, 1, 1, GridConstraints.ANCHOR_CENTER, GridConstraints.FILL_NONE,
                                                  GridConstraints.SIZEPOLICY_FIXED, GridConstraints.SIZEPOLICY_FIXED, null, null, null, 0,
                                                  false));
      myPlayerVersionMinorTextField = new JTextField();
      myPlayerVersionMinorTextField.setColumns(3);
      myPlayerVersionMinorTextField.setHorizontalAlignment(0);
      myMainPanel.add(myPlayerVersionMinorTextField,
                      new GridConstraints(0, 2, 1, 1, GridConstraints.ANCHOR_CENTER, GridConstraints.FILL_HORIZONTAL,
                                          GridConstraints.SIZEPOLICY_CAN_GROW, GridConstraints.SIZEPOLICY_FIXED, null, null, null, 0,
                                          false));
      final JLabel label2 = new JLabel();
      label2.setAlignmentX(0.5f);
      Font label2Font = this.$$$getFont$$$(null, Font.BOLD, -1, label2.getFont());
      if (label2Font != null) label2.setFont(label2Font);
      label2.setText(".");
      myMainPanel.add(label2, new GridConstraints(0, 3, 1, 1, GridConstraints.ANCHOR_CENTER, GridConstraints.FILL_NONE,
                                                  GridConstraints.SIZEPOLICY_FIXED, GridConstraints.SIZEPOLICY_FIXED, null, null, null, 0,
                                                  false));
      myPlayerVersionRevisionTextField = new JTextField();
      myPlayerVersionRevisionTextField.setColumns(3);
      myPlayerVersionRevisionTextField.setHorizontalAlignment(0);
      myMainPanel.add(myPlayerVersionRevisionTextField,
                      new GridConstraints(0, 4, 1, 1, GridConstraints.ANCHOR_CENTER, GridConstraints.FILL_HORIZONTAL,
                                          GridConstraints.SIZEPOLICY_CAN_GROW, GridConstraints.SIZEPOLICY_FIXED, null, null, null, 0,
                                          false));
    }
  }

  /** @noinspection ALL */
  private Font $$$getFont$$$(String fontName, int style, int size, Font currentFont) {
    if (currentFont == null) return null;
    String resultName;
    if (fontName == null) {
      resultName = currentFont.getName();
    }
    else {
      Font testFont = new Font(fontName, Font.PLAIN, 10);
      if (testFont.canDisplay('a') && testFont.canDisplay('1')) {
        resultName = fontName;
      }
      else {
        resultName = currentFont.getName();
      }
    }
    Font font = new Font(resultName, style >= 0 ? style : currentFont.getStyle(), size >= 0 ? size : currentFont.getSize());
    boolean isMac = System.getProperty("os.name", "").toLowerCase(Locale.ENGLISH).startsWith("mac");
    Font fontWithFallback = isMac
                            ? new Font(font.getFamily(), font.getStyle(), font.getSize())
                            : new StyleContext().getFont(font.getFamily(), font.getStyle(), font.getSize());
    return fontWithFallback instanceof FontUIResource ? fontWithFallback : new FontUIResource(fontWithFallback);
  }

  /** @noinspection ALL */
  public JComponent $$$getRootComponent$$$() { return myMainPanel; }

  public String getPlayerVersion() {
    final String major = myPlayerVersionMajorTextField.getText().trim();
    final String minor = myPlayerVersionMinorTextField.getText().trim();
    final String revision = myPlayerVersionRevisionTextField.getText().trim();
    return major +
           ((!minor.isEmpty() || !revision.isEmpty()) ? "." + (!minor.isEmpty() ? minor : "0") : "") +
           (!revision.isEmpty() ? "." + revision : "");
  }

  public String getPlayerVersionMajor() {
    return myPlayerVersionMajorTextField.getText().trim();
  }

  public String getPlayerVersionMinor() {
    return myPlayerVersionMinorTextField.getText().trim();
  }

  public String getPlayerVersionRevision() {
    return myPlayerVersionRevisionTextField.getText().trim();
  }


  public void setPlayerVersion(String playerVersion) {
    final String[] strings = playerVersion == null ? ArrayUtilRt.EMPTY_STRING_ARRAY : playerVersion.split("[.]");
    myPlayerVersionMajorTextField.setText(strings.length > 0 ? strings[0] : "");
    myPlayerVersionMinorTextField.setText(strings.length > 1 ? strings[1] : "");
    myPlayerVersionRevisionTextField.setText(strings.length > 2 ? strings[2] : "");
  }

  public JComponent getMainPanel() {
    return myMainPanel;
  }

  public void setEnabled(final boolean enabled) {
    myMainPanel.setEnabled(enabled);
  }

  public boolean isEnabled() {
    return myMainPanel.isEnabled();
  }

  public void addDocumentListener(final DocumentListener listener) {
    myPlayerVersionMajorTextField.getDocument().addDocumentListener(listener);
    myPlayerVersionMinorTextField.getDocument().addDocumentListener(listener);
    myPlayerVersionRevisionTextField.getDocument().addDocumentListener(listener);
  }

  public void removeDocumentListener(final DocumentListener listener) {
    myPlayerVersionMajorTextField.getDocument().removeDocumentListener(listener);
    myPlayerVersionMinorTextField.getDocument().removeDocumentListener(listener);
    myPlayerVersionRevisionTextField.getDocument().removeDocumentListener(listener);
  }
}
